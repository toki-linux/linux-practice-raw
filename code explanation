## nginx監視スクリプト

### 目的
nginxが停止していないかの確認んと停止していたら自動で起動する

### 処理の流れ
1. nginxの状態を確認
2. 停止していたら起動
3. 状態をログに出力

### 使用コマンド
systemctl is-active
echo
systemctl start

### ポイント
- 上書きでなく追記を使う
- $(date)は「"」で囲むと展開できる
-「--quiet」は余計な出力をしたくないときに使う。終了ステータスだけ

  
  
  
